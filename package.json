{
  "name": "localvault",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check api/bootstrap.js && node --check api/listings.js && node --check api/offers.js && node --check api/leads.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
